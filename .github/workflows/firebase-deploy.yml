name: Deploy to Firebase Hosting

on:
  push:
    branches: [main]
    paths:
      - 'git/issys-eer/public/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ISSYS_EER }}
          projectId: issys-eer
          channelId: live
          entryPoint: ./git/issys-eer
